<plugins>
  <plugin id="io.devdepot.conductor"
          url="https://github.com/devdepot-ai/conductor/releases/download/v0.2.0/conductor-0.2.0.zip"
          version="0.2.0">
    <idea-version since-build="241" until-build="999.*"/>
    <name>Conductor</name>
    <vendor>devdepot</vendor>
    <description>Turn each AI coding session into an isolated workspace: a git worktree, its own IDE window, and a terminal tab running your configured startup command. One workspace = one branch, one agent, zero cross-talk.</description>
  </plugin>
</plugins>
